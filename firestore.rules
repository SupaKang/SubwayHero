rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // 개발 중 테스트를 위해 모든 읽기/쓰기 허용 (실제 배포시 반드시 수정 필요)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}